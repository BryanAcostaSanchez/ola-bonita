"use client";
import { useEffect, useState } from "react";
type Slot={starts_at:string;ends_at:string;remaining_capacity:number};
const mexicoDate=()=>new Intl.DateTimeFormat("en-CA",{timeZone:"America/Mexico_City",year:"numeric",month:"2-digit",day:"2-digit"}).format(new Date());
export function CabinBooking({priceCents,depositEnabled,depositPercent}:{priceCents:number;depositEnabled:boolean;depositPercent:number}){const [date,setDate]=useState(mexicoDate());const [slots,setSlots]=useState<Slot[]>([]);const [selected,setSelected]=useState("");const [form,setForm]=useState({fullName:"",phone:"",email:""});const [message,setMessage]=useState("");const [busy,setBusy]=useState(false);useEffect(()=>{fetch(`/api/cabina/availability?date=${date}`).then(r=>r.json()).then(d=>setSlots(d.slots||[]));},[date]);const reserve=async()=>{if(!selected||!form.fullName||!form.phone)return setMessage("Elige un horario y completa tus datos.");setBusy(true);const r=await fetch("/api/cabina/reservations",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...form,startsAt:selected})});const d=await r.json();if(r.ok&&d.checkout_url)window.location.assign(d.checkout_url);else setMessage(r.ok?`Reserva confirmada. Código: ${d.reservation.public_code}`:d.error);setBusy(false)};const deposit=Math.round(priceCents*depositPercent/100);return <div className="cabin-booking"><label>Fecha<input type="date" min={mexicoDate()} value={date} onChange={e=>{setSelected("");setDate(e.target.value)}}/></label><div className="cabin-slots">{slots.length?slots.map(slot=><button type="button" className={selected===slot.starts_at?"selected":""} onClick={()=>setSelected(slot.starts_at)} key={slot.starts_at}>{new Intl.DateTimeFormat("es-MX",{hour:"2-digit",minute:"2-digit",timeZone:"America/Mexico_City"}).format(new Date(slot.starts_at))}</button>):<p>Sin horarios disponibles.</p>}</div><label>Nombre completo<input placeholder="Tu nombre" value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})}/></label><label>Teléfono<input placeholder="Tu teléfono" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>Correo (opcional)<input placeholder="correo@ejemplo.com" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label>{depositEnabled&&<p className="deposit-note">Al confirmar pagarás un apartado de ${(deposit/100).toFixed(0)} MXN por Mercado Pago.</p>}<button className="button" disabled={busy} onClick={reserve}>{busy?"Reservando…":depositEnabled?"Continuar a pago":"Confirmar reserva"}</button>{message&&<p className="access-message">{message}</p>}</div>}
